# Sin esta variable la app corre 100% local (inventario en localStorage).
# Cuando exista el backend Mongo, apunta aquí su URL base para activar el modo HTTP.
# VITE_API_URL=http://mi-backend:3090/api
